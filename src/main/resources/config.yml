
icons:
  placeholderSupport: true

crafts:
  defaults:
    onlyItemStacks: true # Recipe Choices will only trigger by item stack and not material
  disabledCrafts:
    - iron_block

block:
  sounds:
    stepSound:
      enabled: true
    hitSound:
      enabled: true
    fallSound:
      enabled: true

kick-on-decline:
  enabled: true
  bypassPermission: "Mauris.bypassDeclineRP"
  message: "accept the resourcepack please"
  failureMessage: "strange........."

self-host:
  overrideUrl: "https://frp.plo.su/rp.zip"
  enabled: true
  ip: localhost
  ports:
    - 8095